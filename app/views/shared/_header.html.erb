<header class="site-header">
  <div class="container d-flex align-items-center justify-content-between">
    <div class="brand">
      <%= link_to 'Student Management System', root_path, class: 'brand-link text-decoration-none' %>
      <% if user_signed_in? %>
        <small class="muted">Logged in as: <%= current_user.email %> (<%= current_user.role %>)</small>
      <% end %>
    </div>
    <div>
      <%# placeholder for optional top actions %>
    </div>
  </div>
</header>